(()=>{const nativeFetch=window.fetch.bind(window);window.fetch=(input,init={})=>{const url=typeof input==='string'?input:(input&&input.url)||'';if(!url.includes('raw.githubusercontent.com'))return nativeFetch(input,init);const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),5000);const signal=init.signal||controller.signal;return nativeFetch(input,{...init,signal}).finally(()=>clearTimeout(timer));};setTimeout(()=>{const start=document.getElementById('start'),state=document.getElementById('load-state'),fatal=document.getElementById('fatal'),msg=document.getElementById('fatal-msg');if(start&&start.disabled){if(state)state.textContent='Switching to compatibility mode…';setTimeout(()=>{if(start.disabled&&fatal){fatal.style.display='block';if(msg)msg.textContent='The 3D engine did not initialise. Reload once; the game will use its built-in character if the external animated model is unavailable.';}},3500);}},8000);})();